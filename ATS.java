/*public class ATS {
    public static int calculateATS(Resume resume) {
        int score = 0;
        String[] keywords = {"Java", "AI", "Data", "Analytics", "IoT"};

        for (String keyword : keywords) {
            if (resume.getSkills().toLowerCase().contains(keyword.toLowerCase())) {
                score += 20;
            }
        }
        return score;
    }
}*/
